<UserControl x:Class="SoundLogPro.Desktop.Views.OffloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SoundLogPro.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Zone Source -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="20">
            <StackPanel>
                <TextBlock Text="SOURCE" Style="{StaticResource MaterialDesignOverlineTextBlock}" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Margin="0,0,20,0" ToolTip="Choisir Source">
                        <materialDesign:PackIcon Kind="FolderOpen" />
                    </Button>
                    <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Chemin de la source" VerticalAlignment="Center" FontSize="16"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Zone Destinations -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,20" Padding="20">
            <StackPanel>
                <TextBlock Text="DESTINATIONS" Style="{StaticResource MaterialDesignOverlineTextBlock}" Margin="0,0,0,10"/>
                
                <!-- Destination A -->
                <Grid Margin="0,0,0,15">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Grid.Column="0" Kind="Harddisk" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Destination A" VerticalAlignment="Center"/>
                </Grid>

                 <!-- Destination B -->
                <Grid>
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Grid.Column="0" Kind="HarddiskPlus" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Destination B (Optionnel)" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Spacer -->
        <Border Grid.Row="2"/>

        <!-- Zone Action -->
        <StackPanel Grid.Row="3">
            <Button Content="DÉMARRER LA COPIE" Height="50" FontSize="18" Margin="0,0,0,20" Style="{StaticResource MaterialDesignRaisedAccentButton}"/>
            <ProgressBar Height="20" Value="0" Maximum="100"/>
        </StackPanel>
    </Grid>
</UserControl>
